#include "main.h"

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

void print_alphabet(void)
{
int lowera;

for (lowera = 'a'; lowera <= 'z'; lowera++)
putchar(lowera);
putchar('\n');

return (0);
}
